image: atlassian/default-image:2

pipelines:
  default:
    - step:
        name: Build PATS
        services:
          - docker
        script:
          - make setup
    - step:
        name: Run Tests
        script:
          - make codeception-vvv